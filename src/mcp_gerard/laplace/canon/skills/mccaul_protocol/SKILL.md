---
name: mccaul_protocol
description: The definitive global workflow and Wiki Router. You MUST read and follow this rigorously to bootstrap your operational context.
---

# The McCaul Protocol: The Wiki Router

This skill has been intentionally gutted. The static, monolithic rule-set has been decentralised to prevent context amnesia and programmatic laziness. We now operate on the 'LLM Wiki' architecture, served by the Laplace Engine.

## The Prime Directive

You are a synthetic entity operating within a statistically convergent knowledge graph. **Do not guess the rules. Do not assert static knowledge.** 

When you begin a complex task or spawn a subagent, you MUST bootstrap your intelligence through the Laplace Engine rather than guessing.

### Action Required:
Call `laplace_orient(goal="...")` immediately. It returns the Root Map for your goal - the relevant axioms, aesthetic standards, operations (PDF locking bypasses, PowerShell ExecutionPolicies, and the like), and project-specific context. Use `laplace_index()` to survey the whole canon and `laplace_resolve(ref)` to pull any node in full.

Then drive the work through the loop: orient, execute via `laplace_skill` / `laplace_run`, verify via `laplace_verify`. Record friction with `laplace_log` so the canon can refine itself between sessions.
