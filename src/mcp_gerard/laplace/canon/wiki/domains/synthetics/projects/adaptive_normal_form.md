# Adaptive Normal Form

**Stage**: Foundry, results phase (pre-draft).
**Live state — single source of truth**: the per-result ladder status and the one next action live in the project's `HANDOFF.md`, never restated here. This node holds only what is durable across sessions. For where the work *is* — which results are settled, what is active, what to do next — read `physics_paper_orchestra/adaptive-normal-form/HANDOFF.md`. Do not infer current result status from this canon node.
**Root**: `physics_paper_orchestra/adaptive-normal-form/`. Start at its `HANDOFF.md`, then the ledgers in `inputs/` and `workflow/state/`.
**Thesis**: there is a canonical normal form for adaptive multiscale systems, and its dynamical phases and universality classes are governed by one object - the order-parameter susceptibility exponent at the self-similar fixed point. The dynamical phase transition is the shadow of the renormalisation fixed point.
**Scope commitment**: universality is structural - any system satisfying the model's assumptions is a member of the class - with disciplinary cases as illustration, not an empirical cross-domain measurement programme.
**Relationship to The Phases of Hierarchy**: that prior project is quarantined reference, a single worked corner of this classification. Results enter here only by fresh re-derivation with provenance noted (a reference_archaeologist pass).
**Subagent directive**: load the handoff brief and the `inputs/` ledgers first, work one result at a time against the result ladder, and send strays to the backlog rather than the active path.
